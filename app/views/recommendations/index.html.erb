<h1 class="text-center text-4xl font-bold mt-10 mb-10">ROCKのおすすめ曲をチョイスしました</h1>
<div class="flex flex-wrap justify-center gap-4 md:gap-8">
  <% @tracks.each do |track| %>
    <div class="w-1/3 p-2 md:p-4">
      <strong><%= track[:name] %></strong> by <%= track[:artist] %>
      <% if track[:album_image_url] %>
        <img src="<%= track[:album_image_url] %>"width="300" height="300" class="block" alt="Album Image">
      <% else %>
        <p>No album image available</p>
      <% end %>
      <% if track[:preview_url] %>
        <audio controls>
          <source src="<%= track[:preview_url] %>" type="audio/mpeg">
          Your browser does not support the audio element.
        </audio>
      <% else %>
        <p>No preview available</p>
      <% end %>
    </div>
  <% end %>
</div>









